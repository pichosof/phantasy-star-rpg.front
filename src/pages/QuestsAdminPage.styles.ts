import type { CSSProperties } from 'react';

export const restrictedTitle: CSSProperties = {
  marginTop: 0,
};

export const searchField: CSSProperties = {
  maxWidth: 380,
};

export const createForm: CSSProperties = {
  display: 'grid',
  gap: 10,
  maxWidth: 920,
};

export const statusSelect: CSSProperties = {
  minWidth: 220,
};

export const mobileCardsGrid: CSSProperties = {
  display: 'grid',
  gap: 12,
  gridTemplateColumns: '1fr',
};

export const adminTable: CSSProperties = {
  minWidth: 1020,
};

export const helperText: CSSProperties = {
  display: 'block',
  marginTop: 8,
};

export const helperCard: CSSProperties = {
  marginTop: 12,
};

export const drawerStatusSelect: CSSProperties = {
  minWidth: 240,
};

export const drawerDivider: CSSProperties = {
  margin: '6px 0',
};

export const previewParagraph: CSSProperties = {
  whiteSpace: 'pre-wrap',
  margin: 0,
};
